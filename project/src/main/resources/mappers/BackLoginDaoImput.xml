<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blogs.mydlogsdemo.dao.BackLoginDaoImput">
    <select id="examineID" parameterType="com.blogs.mydlogsdemo.domain.BackLoginID"  resultType="com.blogs.mydlogsdemo.domain.BackLoginID">
          SELECT * FROM backgrounduser WHERE userName=#{userName} AND userPwd=#{userPwd};
        </select>

</mapper>